rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read and write access to all collections
    // Note: In production, you should restrict this based on authentication
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
